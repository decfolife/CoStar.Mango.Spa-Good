# Token Library

This library its for the exclusive use of the SASS configurations and styling.

*Don't add components to this library*.

For detailed documentation on how to use please serve `storybook-host`.

This was generated using [Nx](https://nx.dev).

## Running unit tests
~~~~~~~~
Run `nx test ui-shared-common` to execute the unit tests.