export enum EmailApprovalPreference {
  Enabled = 1,
  Disabled = 0,
}

export enum CalculateDatesPreference {
  Enabled = 1,
  Disabled = 0,
}

export enum AutoCalculatePreference {
  Enabled = 1,
  Disabled = 0,
}

export enum AddSubTask {
  TRUE = 1,
  FALSE = 0,
}
