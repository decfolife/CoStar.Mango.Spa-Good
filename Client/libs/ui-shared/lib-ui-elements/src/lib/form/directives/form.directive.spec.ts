import { CremFormDirective } from './form.directive';

describe('FormDirective', () => {
  it('should create an instance', () => {
    const directive = new CremFormDirective();
    expect(directive).toBeTruthy();
  });
});
