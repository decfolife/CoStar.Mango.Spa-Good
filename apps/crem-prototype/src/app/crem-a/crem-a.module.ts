import { NgModule } from '@angular/core';
import { SharedModule } from '../shared_modules/shared.module';
import { SharedComponentsModule } from '../shared_modules/shared-components.module';

@NgModule({
	declarations: [
	],
	imports: [
		SharedModule,
		SharedComponentsModule,
	]
})
export class CremAModule { }
